# Single shared static API key for the MCP server (v1).
# If unset, the code falls back to a hardcoded default (see api/mcp.ts).
# Set this in the Vercel project env (Production + Preview) to rotate the key.
MCP_API_KEY=replace-with-a-long-random-string
